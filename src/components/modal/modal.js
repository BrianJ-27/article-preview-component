import React from "react";
import styled from "styled-components";

const Modal = styled.div`
  background-color: var(--clr-primary-text);
  color: #ffffff;
`;

const socialModal = () => {
  return <Modal>This is my modal</Modal>;
};

export default socialModal;
